{
  "name": "@ldgr/market-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker caching proxy for ldgr market data (ADR-007)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.9.2",
    "vitest": "^2.1.9",
    "wrangler": "^4.42.0"
  }
}
